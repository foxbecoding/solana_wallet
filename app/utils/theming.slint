import { Solana } from "./structs.slint";

export global AppTheme {
    in property<color> background: rgb(18,18,18);
    in property<color> appbar: rgba(18,18,18,0.95);
    in property<color> bottomNav: rgba(18,18,18,0.95);
    in property<color> surface: rgba(255,255,255,0.08);
}